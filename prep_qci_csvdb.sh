mysql -h $foos.amazonaws.com -u$fooser -p$foow -e 'select * from (seluect * from (select * from (select date ase) FROM qci.cla WHERE t_id=clant_id AND type=cla.type)) c1 join (select ssment as "assessment_qci", inheritance as "inheritance_qci", phenotype as "phenotype_qci", variant_id as "variant_id_qci" from qci clas where type = "EN" and date=(SELECT MAX(date) FROM qon WHERE variant_id=clas.variant_id AND type=clas.type) ) c2 on c1.variant_id_user = c2.variant_id_qci) c join (select chrp_dot, id from qci.variant group by id) v on c.variant_id_user = v.id) c3 join (select variant_id, assessment m_author", date as "com_date" from ****)) com on com.variant_id = c3.variant_id_user' qci > qci_1202.csv
